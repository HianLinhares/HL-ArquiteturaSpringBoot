spring:
  application:
    name: HLSpringBoot
  datasource:
    url: jdbc:postgresql://localhost:5432/library
    username: postgres
    password: "@123"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
